# Colab URL (обязательно, меняется при каждом рестарте Colab)
AGENT_COLAB_URL=https://твой-туннель.trycloudflare.com

# Groq API Key (опционально, fallback)
AGENT_GROQ_API_KEY=твой_ключ

# Лимиты
AGENT_MAX_STEPS=30
AGENT_APPROVAL_MODE=ask   # ask | auto | read-only
AGENT_REQUEST_TIMEOUT=120
